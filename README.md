```
npm i
npm run dev
```

---

1. `cna` (createNextApp)
2. `cc` (createConfig)
3. `scni` (initShadcnui)
4. remove `lucide-react`
5. `scna` (addShadcnui)

To do:

- `<Icon/>` (Material Symbols SVGs)
- move shadcn dependencies to dev dep?
